This project was built with the microsoft-foundry skill. Before working on or answering questions about foundry agents, read the microsoft-foundry skill first.

The workshop uses synthetic data only. Never change shared Azure resources or
the default CLI subscription. Pass the configured subscription explicitly.
Preserve model, prompt, dataset and trace lineage. Never substitute another
model or count missing/error evaluation rows as successful.

Run `python -m unittest discover -s tests -v` before cloud execution.
Keep the Korean guide aligned with actual command behavior and record real
cloud verification separately in `docs/validation.ko.md`.
