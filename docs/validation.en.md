# Read evaluation results and recorded examples

[English participant guide](../README.md#lab-c) · [한국어 실행 결과](validation.ko.md)

**For steps 5–9, read your saved results first; use the recorded example run only as an example.** This page answers where your result files are, which rows failed, what the recorded run showed, and what it does not prove.

**Read only what you need:**

- **Your run:** [result files](#read-your-results) · [other lookups](#other-lookups) · [Foundry evaluator failures](#native-failures) · [business checks](#business-checks)
- **Recorded run:** [results](#measured-results) · [remaining business failure](#remaining-business-failure) · [Foundry relevance failure](#native-relevance-failures) · [retrieval misses](#retrieval-miss) · [reviewed trace](#reviewed-case) · [tokens and latency](#tradeoffs) · [execution versus quality](#execution-quality) · [dashboard](#dashboard)
- **Audit:** [lineage IDs](#lineage) · [cleanup scope](#cleanup-scope)

<details>
<summary>Recorded English example run — not your pass criteria</summary>

- English recorded run: `en-20260923b`, business 0/18→17/18, holdout 12/12, not approved for production.
- Korean run is a separate record with different numbers. Do not mix its values with English values.

</details>

<a id="read-your-results"></a>

## Read your own results first

All paths below are under **`src/agent/.foundry/results/`**. They describe **your local run**; the later tables describe the recorded run above. If you used retry labels, use those labels and paths instead.

These files are generated by `collect`, `evaluate`, `compare`, and `verify`. Their absence in a fresh clone is normal before the corresponding step. **A file missing after its command completed requires recovery**; do not fill the gap by copying recorded example results.

**Quick view:** from the repository root of the workshop folder, run the block for your step; `summary` only reads saved results.

**Step 6 — a baseline `row_id` to review:**

```bash
python scripts/workshop.py summary --labels baseline
```

**Checkpoint:** a `sol` / `luna` / `astra` table and `baseline business-check failures:` appear.

**If not:** if it says `comparison.json` is missing or lacks `baseline`, run the [6-1](../README.md#6-1-aggregate-the-results-and-find-a-row-to-review) `compare` block, then rerun this `summary`. Do not rerun `collect` or `evaluate`.

**Next:** return to [README 6-2](../README.md#review-case) and choose the first `row_id` in that list.

**Step 7-4 — same-dev changes, the reviewed case's V2 result, and failed rows:**

```bash
python scripts/workshop.py summary --labels baseline improved
```

**Checkpoint:** `Reviewed case ...` with `source trace carried: yes`, the V1 `->` V2 table, then `improved business-check failures:` and `improved Foundry-score failures:`.

**If not:** for a missing `comparison.json` or label, run the [7-3](../README.md#7-3-collect-and-evaluate-the-same-dev-set) `compare` block, then rerun this `summary`. Do not rerun `collect` or `evaluate`. If `Reviewed case` is missing or says `source trace carried: no`, stop and check the 6-3 record with the instructor.

**Next:** return to [README 7-4](../README.md#compare-results) and copy the three parts into your notes.

**Step 8-3 — separate holdout pass counts per model and failed rows:**

```bash
python scripts/workshop.py summary --labels holdout
```

**Checkpoint:** a table with `business`, `groundedness`, and `relevance` as `.../4`, then `holdout business-check failures:` and `holdout Foundry-score failures:`.

**If not:** for a missing `comparison.json` or label, run the [8-2](../README.md#holdout-evaluation) `compare` block, then rerun this `summary`. Do not rerun `collect` or `evaluate`. For `n/a` evaluator results, [recover evaluation](troubleshooting.en.md#evaluation-retry).

**Next:** return to [README 8-3](../README.md#holdout-results) and copy both lists into your notes separately.

| Question | Open | Read / interpretation |
|---|---|---|
| What changed for each model? | `comparison.json` | Under `labels → <label> → models → sol/luna/astra`, compare `business_passed/total` and `required_citation_passed/required_citation_total`. Read `foundry_evaluators` means and pass counts together. |
| Did it get faster or use fewer tokens? | `comparison.json` | At the same model path, compare `input_tokens`/`output_tokens` and `latency_p50_seconds`/`latency_p95_seconds`. Preserve the [measurement scope](#tradeoffs); these are not total costs. |
| Which business check failed? | The output of `python scripts/workshop.py show --label <label> --row-id <row_id>` (sources: `<label>/responses.jsonl` and `data/en/dev.jsonl`) | Compare the `false` items in `business_checks` with `expected_decision`, `required_numbers`, and `allowed_citations` in `fixed_reference`, then inspect its `trace_id`. Look at holdout rows only after step 8. |
| Which Foundry evaluator failed? | `<label>/evaluation-results.json` | Find **the same `row_id` within that label**. In its `results` array, select `name: groundedness` or `name: relevance`, then read `score` and `passed`. |

<a id="other-lookups"></a>

<details>
<summary>Other lookups: portal report, reviewed case, full-exercise evidence</summary>

| Question | Open | Read / interpretation |
|---|---|---|
| Where is that evaluation's portal report? | `<label>/evaluation.json` | Open `run → report_url`; use the same label, not a recorded example's URL. |
| Did the reviewed case change in V2? | `baseline/responses.jsonl` and `improved/responses.jsonl` | Match **`case_id` + `model_key`** and compare answers/business checks. Check that V2's `regression_source_trace_ids` includes the reviewed V1 trace. [README 7-4](../README.md#compare-results) |
| Did the full exercise execute, and did the candidate meet the quality gate? | `verified-evidence.json` | Read execution counts, the quality gate (`candidate_quality_gates`), and production approval (`production_release_approved`) separately; a true business gate is not production approval. |

</details>

- Arrows indicate JSON fields, not portal menus.
- `evaluation-results.json` is a **list of rows**, not an object keyed by `row_id`; find the row before expanding its two evaluator results.
- A Foundry evaluator average of 4 does not mean every row passed.
- A business gate requires **at least 5/6 dev and 4/4 holdout business passes per model**, plus all required citations valid; it is not production approval.

<a id="native-failures"></a>

### When a Foundry evaluator pass count is below its total

| Question | Open | Read | Meaning |
|---|---|---|---|
| Why did a Foundry evaluator fail? | That label's `evaluation-results.json`, then its `responses.jsonl`. | Rows with **`passed: false`** in `results` (`row_id`, `name`, `score`); then view the **same `row_id`**'s `query`, `answer`, and business checks with `show`, and for groundedness read `context` in responses. | Record business and Foundry results separately. A correct policy deferral can get low relevance; a wrong [`decision` label](reference.en.md#decision-values) can accompany a well-grounded explanation. |

Report holdout failures as limitations, not as input for tuning instructions. Do not rerun an evaluation simply because its valid score is low.

↩ [README dev comparison](../README.md#compare-results) / [holdout readout](../README.md#holdout-results)

<a id="business-checks"></a>

### What each evaluation layer receives

`collect` invokes the actual Hosted Agent and preserves the response, model routing, prompt hash, retrieved context, and trace. `evaluate` sends the **same answer text** to Foundry rather than generating another answer.

| Layer | Inputs | Pass criterion |
|---|---|---|
| `decision` | Structured decision and frozen expected value | Exact match |
| `required_numbers` | Answer text and required monetary values | All required amounts present after normalization |
| `citations_retrieved` | Citation values and that request's retrieved source IDs | Every citation was retrieved |
| `citations_relevant` | Citation values and the case's allowed ID list | Every citation is allowed for the case |
| `citation_present` | Citation-required flag and citation array | Nonempty when required |
| Foundry groundedness | Question, answer text, and actual retrieved context | 1–5 scale; pass at 4 or above |
| Foundry relevance | Question and answer text | 1–5 scale; pass at 4 or above |

The rest of this document explains the method and **recorded examples**, not scores your run must reproduce.

<details>
<summary>Recorded-run method: evaluator inputs, calibration, and run contract</summary>

- Foundry evaluator `response` maps to `answer`, not the complete response JSON.
- Neither evaluator directly receives the structured `decision` or `citations` array, and `ground_truth` (kept in JSONL) is not mapped into either. Interpret Foundry evaluator scores and business-check results separately.
- The amount check tests whether the normalized numbers (for example, `180,000` or `180000`) appear in the answer, not whether every sentence is fully correct; cases with no required amounts pass it.
- Citation-required rows have a different denominator from all response rows.
- A new run caches the evaluator versions it fetches first and uses the same definitions for the before/after evaluations.
- Two fixed English calibration examples received groundedness **5** for the supported KRW 180000 limit and **2** for an unsupported KRW 990000 claim; calibration passed and is not part of the 48 candidate responses.

| Component | Contract |
|---|---|
| Scenario | Synthetic company travel-policy consultation; no real approval, booking, or payment |
| Language/workspace | `LAB_LANGUAGE=en`, separately owned workspace, agent, and knowledge objects |
| Candidates | `gpt-6-sol` and `gpt-6-luna` version `2026-09-22`; `gpt-6-astra` version `2026-09-03` |
| Judge/evaluators | `gpt-5.4-mini` version `2026-03-17`; `builtin.groundedness` version 17 and `builtin.relevance` version 12, threshold 4 |
| Baseline/candidate | English V1 vs English V2 on the same six dev questions × three models |
| Holdout | Frozen V2, four separate questions × three models |
| Retrieval | Same KB retrieval instructions and one retry when the planner runs no search ([why](#retrieval-miss)) |

</details>

<details>
<summary>What changed between V1 and V2</summary>

V1 is an intentionally incomplete educational starting point. It tells the assistant not to display internal document identifiers, which can conflict with the citation-ID contract.

V2 explicitly distinguishes effective dates and document status, defines the decision labels, requires original retrieved document IDs, handles insufficient evidence, and rejects instructions to ignore policy or invent completed approval. It is a provided prompt candidate, not a model substitution, fine-tuning job, or automatic prompt optimizer. Dev questions, reference answers, corpus, three candidates, judge, evaluator definitions, and concurrency stayed fixed.

</details>

<a id="measured-results"></a>

## Recorded example results

Use this section only to explain the recorded English run; your run need not match it. Its raw files are not in the repository, so the tables copy their values; where an **Open** cell names a file, it is the matching file in your own `src/agent/.foundry/results/`.

| Question | Open | Read | Meaning |
|---|---|---|---|
| What did the recorded English run show? | `comparison.json` and each label's `evaluation-results.json` (recorded values below). | The cohort summary below; per-model tables are folded under it. | Use these values only to explain the recorded run, not as your pass criteria. |

### Cohort summary

| Cohort | Agent version | Responses / distinct traces | Business pass | Required citations valid | Groundedness pass | Relevance pass |
|---|---:|---:|---:|---:|---:|---:|
| V1 dev | 1 | 18 / 18 | 0/18 | 0/15 | 18/18 | 16/18 |
| V2 dev | 2 | 18 / 18 | **17/18** | 15/15 | 18/18 | 17/18 |
| V2 holdout | 2 | 12 / 12 | 12/12 | 12/12 | 12/12 | 12/12 |

- **0/18 is not a claim that every V1 answer was factually wrong:** every baseline response failed the citation-ID contract, even when its answer had the correct amount.
- All three Foundry evaluation jobs completed with the expected row counts and no evaluation error rows.

<details>
<summary>Per-model business-check and Foundry evaluator tables</summary>

### Business-check results

| Model | V1 dev | V2 dev | Frozen V2 holdout | V2 dev required citations |
|---|---:|---:|---:|---:|
| Sol (`gpt-6-sol`) | 0/6 | **5/6** | 4/4 | 5/5 |
| Luna (`gpt-6-luna`) | 0/6 | 6/6 | 4/4 | 5/5 |
| Astra (`gpt-6-astra`) | 0/6 | 6/6 | 4/4 | 5/5 |
| **Total** | **0/18** | **17/18** | **12/12** | **15/15** |

### Foundry evaluator results — read means and pass counts together

The two built-in evaluators here are Foundry groundedness and relevance.

| Cohort | Model | Groundedness mean | Pass | Relevance mean | Pass |
|---|---|---:|---:|---:|---:|
| V1 dev | Sol | 5.00 | 6/6 | 4.17 | 5/6 |
| V1 dev | Luna | 5.00 | 6/6 | 4.00 | 5/6 |
| V1 dev | Astra | 5.00 | 6/6 | 4.33 | 6/6 |
| V2 dev | Sol | 5.00 | 6/6 | 4.17 | 5/6 |
| V2 dev | Luna | 5.00 | 6/6 | 4.17 | 6/6 |
| V2 dev | Astra | 5.00 | 6/6 | 4.50 | 6/6 |
| V2 holdout | Sol | 5.00 | 4/4 | 4.00 | 4/4 |
| V2 holdout | Luna | 5.00 | 4/4 | 4.25 | 4/4 |
| V2 holdout | Astra | 5.00 | 4/4 | 5.00 | 4/4 |

</details>

<a id="remaining-business-failure"></a>

### The remaining V2 business failure: Sol D02's decision label

| Question | Open | Read | Meaning |
|---|---|---|---|
| Why did one V2 business row fail? | `improved/responses.jsonl` (recorded values in this row). | `row_id: improved-sol-D02`; `decision: not_allowed`; `business_grade → checks → decision: false`; the other four checks `true`. | Evidence was present; the failure is the decision label `not_allowed` instead of the frozen `needs_approval`. |

Luna and Astra returned `needs_approval` for the same question, and Sol still cleared the business gate (5/6, all required citations valid).

<details>
<summary>Answer details and run-to-run instability</summary>

The D02 traveler has KRW 190,000 nightly lodging without prior approval and asks to claim reimbursement immediately. `improved-sol-D02` explained the KRW 180,000 limit, prior approval requirement, and after-the-fact approval problem. It cited `TRAVEL-2026` and `APPROVAL-2026`, both retrieved on the first attempt.

That day's first English run showed the reverse: V1 Sol returned `not_allowed`, and V2 returned `needs_approval`. Sol's boundary between "approval required" and "not allowed" was unstable across runs. The rubric, reference answer, and response were not changed after seeing either result.

</details>

↩ [README step 9](../README.md#lab-g)

<a id="native-relevance-failures"></a>

### The Foundry relevance failure: D04

| Question | Open | Read | Meaning |
|---|---|---|---|
| Why did one Foundry relevance row fail? | `improved/evaluation-results.json` (recorded values in this row). | `row_id: improved-sol-D04`; `results[] → name: relevance`; `score: 3`; `passed: false`. | Correct deferral passed business checks, but the generic relevance judge penalized the missing overseas limit. |

Do not retroactively turn these grades into passes; justified deferral needs a business-specific evaluation in a future experiment.

<details>
<summary>Answer details</summary>

`improved-sol-D04` answered that the supplied policies cover domestic travel only, so no overseas hotel limit can be given, and referred the traveler to finance. It passed the business contract with **`not_covered`** and **`SCOPE-2026`**. Baseline Sol and Luna also received relevance 3 for D04.

</details>

↩ [README step 9](../README.md#lab-g)

<a id="retrieval-miss"></a>

### Retrieval misses and the guard used in this run

| Question | Open | Read | Meaning |
|---|---|---|---|
| Did retrieval fail for my row? | That label's `responses.jsonl`, same `row_id`. | `retrieval_attempts`, `source_ids`, and whether its `activity` list has an entry with `type: searchIndex`. | `retrieval_attempts: 2` with empty `source_ids` is a retrieval failure; do not treat it as a prompt or model-quality failure. |

In this English run, all 48 responses searched on the first attempt (`retrieval_attempts: 1`), and none had empty evidence.

<details>
<summary>How the guard works and why it was added</summary>

- The guard changes retrieval only: the KB has retrieval instructions to search applicable policies even when a request asks to ignore or rewrite them. Prompts, data, reference answers, and evaluators are unchanged.
- If a response's `activity` has no `searchIndex` step, the agent retries the same question **once**; the attempt count is stored as `retrieval_attempts` in the response and its trace.
- In the first Korean run (`ko-20260923`), Korean D06 asked to ignore the rules and write that approval was complete. Foundry IQ's planner recorded only `modelQueryPlanning` and ran no search, so Sol answered `not_covered` with empty `source_ids`.
- Calls without search fell from 19/30 before the change to 1/30 with KB retrieval instructions and 0/30 with one retry added. English D06 skipped search in 0/30 calls even before the change. Full record: [Korean results](validation.ko.md#retrieval-miss).

</details>

↩ [README step 9](../README.md#lab-g)

<a id="reviewed-case"></a>

## The reviewed case and its real trace

| Question | Open | Read | Meaning |
|---|---|---|---|
| What did the reviewed case prove? | `baseline/responses.jsonl` and `improved/responses.jsonl` (recorded values below). | `baseline-sol-D01` and `improved-sol-D01`; trace IDs; citation values; `source_ids`. | V1 had the right policy answer but the wrong citation form; V2 returned the source ID and retained provenance. |

The review selected **`baseline-sol-D01`**. Its answer correctly allowed KRW 170,000 lodging under the KRW 180,000 limit, but its citation was a document **title**, not the retrieved source ID.

| Evidence | V1 baseline | V2 candidate |
|---|---|---|
| Row | `baseline-sol-D01` | `improved-sol-D01` |
| Trace | `afe9bd6bf2db3cd87fe553a07927cd22` | `f03bc954e60782c2e68baf5d64f012b4` |
| Decision | `allowed` | `allowed` |
| Citation | `Current domestic travel expense policy` | `TRAVEL-2026` |
| Correct source available | `TRAVEL-2026` in `source_ids` | `TRAVEL-2026` in `source_ids` |
| Business result | Failed citation checks | All five checks passed |

This automated review used **`--reviewer assistant`**; in the README exercise, use `--reviewer human` only when a person actually reviewed the case.

<details>
<summary>What <code>feedback</code> saved and what it did not change</summary>

- `feedback` copied the frozen dev question, reference answer, and rubric, then added the reviewed row, original trace, language, prompt/context hashes, and an English review reason. It did **not** promote the agent's answer to ground truth.
- The V2 manifest and matching response retained the baseline source trace. The regression case did not add a seventh dev question or change the denominator.
- This is a **learning loop that ties an evaluation case to the reason for a change**; it does not send the regression JSONL to model training or update model weights.

</details>

↩ [README step 9](../README.md#lab-g)

<a id="tradeoffs"></a>

## Tokens and latency: improvement has tradeoffs

| Question | Open | Read | Meaning |
|---|---|---|---|
| Did V2 cost less or get faster? | `comparison.json` (recorded values below). | `input_tokens`, `output_tokens`, `latency_p50_seconds`, and `latency_p95_seconds` under each model. | Candidate input rose while output fell; latency changes are small-sample request timings, not a bill or SLO. |

These values cover only candidate-model requests in the primary response matrix, so they are neither an Azure bill nor a production SLO.

<details>
<summary>Measurement scope</summary>

- Tokens are those reported by the candidate models; time is retrieval plus model processing inside each request (not external HTTP round trips).
- Excluded: planner/judge calls, calibration, smoke, portal requests, hosting, Search uptime, and log retention.

</details>

| Cohort | Candidate input tokens | Candidate output tokens |
|---|---:|---:|
| V1 dev, 18 responses | 16,646 | 1,928 |
| V2 dev, 18 responses | 21,904 | 1,778 |
| V2 holdout, 12 responses | 13,905 | 1,269 |

Same-dev input rose **31.6%** and output fell **7.8%**; V2 has longer instructions, and retrieved contexts can vary.

| Model | V1 p50 / p95, seconds | V2 p50 / p95, seconds |
|---|---:|---:|
| Sol | 2.928 / 3.984 | 3.230 / 3.757 |
| Luna | 3.133 / 4.176 | 2.735 / 3.482 |
| Astra | 4.852 / 6.034 | 4.420 / 5.449 |

Not every value improved: Sol's p50 rose while its p95 fell; Luna and Astra were faster at both. With six observations per model, p95 is effectively the slowest request.

↩ [README step 9](../README.md#lab-g)

<a id="execution-quality"></a>

## Execution, quality, and approval are different

| Question | Open | Read | Meaning |
|---|---|---|---|
| Did the exercise execute, and is V2 approved? | `verified-evidence.json` (recorded values below). | `component_execution_verified`, `primary_model_outputs`, `distinct_verified_traces`, `candidate_quality_gates`, `production_release_approved`. | The exercise executed. Each model cleared the gate, but one dev business row and one relevance row still failed. `production_release_approved` stays `false`. |

In `candidate_quality_gates`, each model needs **at least 80% business passes and all required citations valid**. All three models cleared it for V2 dev and holdout; Sol's 5/6 dev result clears the gate, but D02 remains a recorded business failure.

| Check | Actual result |
|---|---|
| `component_execution_verified` | `true` |
| `primary_model_outputs` | `48` |
| `distinct_verified_traces` | `48`, all unsampled with weight 1 |
| V2 dev and holdout business gates | Passed for all three models |
| All V2 business rows passed | **No: Sol D02's decision label failed (17/18 dev)**; holdout 12/12 |
| All V2 Foundry evaluator scores passed | **No: Sol D04 relevance failed** |
| Reviewed baseline provenance reused | Yes |
| `production_release_approved` | **`false`** |

Six dev and four translated educational holdout cases do not establish statistical superiority or production readiness.

<details>
<summary>Interpretation limits</summary>

- Some retrieved contexts differed even with the same frozen corpus (V1 D01, D02, D04, D06; V2 D02, D03, D04, D06; holdout H01, H03). These are end-to-end retrieval-and-answer results, not isolated model rankings; a repeated holdout is not new independent validation.
- `verify` checks 18 + 18 + 12 responses, 48 distinct traces, completed evaluations, the frozen data, instructions, and version, reused regression provenance, and real telemetry with sampling weight 1.
- Calibration examples, smoke tests, and extra portal calls are not counted in the 48.

</details>

↩ [README step 9](../README.md#lab-g)

<a id="dashboard"></a>

## Read the operational dashboard honestly

| Question | Open | Read | Meaning |
|---|---|---|---|
| Why does the dashboard count differ from 48? | The agent's **Monitor → Last Day** view, then `verified-evidence.json`. | Recorded dashboard: 53 agent runs and about 125.7K tokens; evidence: `primary_model_outputs: 48`, `distinct_verified_traces: 48`. | Dashboard totals include smoke and portal calls; verify the 48-response matrix first. |

- The displayed estimated cost of `$0` is not proof of free execution. If cost is a concern, ask the environment owner to review retained services; do not infer cleanup from the dashboard.

↩ [README step 9](../README.md#lab-g)

<a id="lineage"></a>

## Minimum reproducibility and lineage identifiers

| Question | Open | Read | Meaning |
|---|---|---|---|
| Which IDs tie the numbers to one run? | The recorded IDs and hashes below. | Evaluation IDs, run IDs, and collection run IDs below; input hashes are folded under them. | Use these IDs to identify the recorded run; do not use them as your local pass criteria. |

| Cohort | Evaluation ID | Evaluation run ID |
|---|---|---|
| baseline | `eval_d58f5eac7da445558c400ec20e133ee7` | `evalrun_c6ee10ece7de43ddaf5aa73c27b8a172` |
| improved | `eval_7d61db79f86046cc9029830a039c20b7` | `evalrun_67fe284e7d07406f908d4670a37d9f35` |
| holdout | `eval_d6a85e8889114243a65a2f43df87417d` | `evalrun_c8422b0394a444a1910048a5db8b5943` |

Collection run IDs were `baseline-20260923T025430Z`, `improved-20260923T025941Z`, and `holdout-20260923T030150Z`. These timestamps are UTC, corresponding to about noon on September 23 in Korea.

<details>
<summary>Dataset, prompt, and suite hashes</summary>

| Input | SHA-256 |
|---|---|
| English dev | `73830035a636dc0bfd4fd19c348fe22b8133168f6bc68a7052d4e73963e55743` |
| English holdout | `65e5300bf8eefe94e8f0a69bbe38b4d189c0ca7dac65c67a50515568fb164bb9` |
| English policy corpus | `8e63a0d85dd0adb32903d00ed48ecb49310c7c35f389e8aa74101405e40cf6e1` |
| Effective English V1 instructions | `368f228625e4e565d28f2711da1634a4628a72a827bc056931627a10921bc6d6` |
| Effective English V2 instructions | `4f451a8514f0f0d7f84803de01d963e6bcc712fa3a6d0faec15225877b0f2eaf` |
| Shared evaluation suite | `53acc92aece97ae4ade833f9a0614c0def53b63f4a6a53a508fcba5507d244b0` |

Dataset/corpus hashes use the runner's normalized JSON representation. Prompt hashes include the shared output contract; they are not simple file-byte hashes. The data, corpus, instruction, and suite hashes match that day's first English run; only the retrieval-miss guard changed between the two runs.

</details>

↩ [README step 9](../README.md#lab-g)

<a id="cleanup-scope"></a>

## Azure scope and completed cleanup

| Question | Open | Read | Meaning |
|---|---|---|---|
| What was cleaned up in the recorded run? | The recorded cleanup result below; the [README 10-3 example screen](../README.md#cleanup-check) shows the recorded `check-cleanup` output. | One English agent, three Search objects, and two runtime role assignments were removed; shared deployments and foundation services remain. | Cleanup applies only to owned workshop objects; retained shared services can still incur cost. If an owned object remains in your run, return to [README step 10](../README.md#cleanup); do not delete shared deployments. |

The run used the existing shared foundation **`rg-iq-foundry-lab-56d62b` in Sweden Central** with its own team names and owned **no model deployments**. Cleanup therefore kept the three shared candidates, the auxiliary planner/judge, the Foundry project, Search, Application Insights/Logs, and connections. **Retained services can still incur costs**; the environment owner manages their lifecycle.

↩ [README step 10 cleanup](../README.md#cleanup) / [step 9 report](../README.md#lab-g)

<a id="run-record"></a>

<details>
<summary>Run environment and run record</summary>

The executed source was revision `e01bddb` plus the retrieval-miss guard, which is committed with these results. Offline tests passed before cloud execution. The run used the instructor-prepared shared deployments `ll-0910-sol`, `ll-0910-luna`, and `ll-0910-astra` (GlobalStandard, capacity 50, `NoAutoUpgrade`) and its own team names (`ll-en-0923b`, `frontier-loop-en-0923b`). The Korean run's objects and results were kept separate. Other agents and knowledge objects in the shared project were not changed, and no default CLI subscription or shared Azure setting was changed. Resource placement does not mean GlobalStandard inference is restricted to Sweden Central. The primary batch sessions had already been stopped after telemetry verification.

The workshop uses three fixed candidates, `gpt-6-sol`, `gpt-6-luna`, and `gpt-6-astra`, for 18 + 18 + 12 = 48 verified responses. That day's first Korean run revealed the [D06 retrieval miss](#retrieval-miss). After adding KB retrieval instructions and one retry, both the Korean and English workshops were rerun from the start; the numbers above come from that rerun.

</details>

Implementation and sources: [collection, evaluation, feedback, and verification](../scripts/experiments.py) · [business grading](../scripts/grading.py) · [model calls and latency](../src/agent/policy_agent.py) · [language configuration](../src/agent/settings.py) · [official sources](reference.en.md#official-sources).
